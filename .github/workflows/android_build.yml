name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Download Gradle Wrapper JAR (if missing)
      run: |
        GRADLE_WRAPPER_JAR="gemini-social-android/gradle/wrapper/gradle-wrapper.jar"
        if [ ! -f "$GRADLE_WRAPPER_JAR" ]; then
          GRADLE_DISTRIBUTION_URL=$(grep 'distributionUrl' gemini-social-android/gradle/wrapper/gradle-wrapper.properties | cut -d'=' -f2 | sed 's/\\//g')
          echo "Downloading Gradle wrapper from $GRADLE_DISTRIBUTION_URL"
          wget -q -O "$GRADLE_WRAPPER_JAR" "$GRADLE_DISTRIBUTION_URL" || curl -sL "$GRADLE_DISTRIBUTION_URL" -o "$GRADLE_WRAPPER_JAR"
        else
          echo "Gradle wrapper JAR already exists."
        fi

    - name: Grant execute permission for gradlew
      run: chmod +x gemini-social-android/gradlew

    - name: Build debug APK
      run: |
        cd gemini-social-android
        ./gradlew assembleDebug

    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-debug-apk
        path: gemini-social-android/app/build/outputs/apk/debug/app-debug.apk
